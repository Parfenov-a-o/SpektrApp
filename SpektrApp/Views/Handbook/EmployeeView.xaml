<Window x:Class="SpektrApp.Views.Handbook.EmployeeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpektrApp.Views.Handbook"
        mc:Ignorable="d"
        Title="Сотрудники" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <DataGrid MaxHeight="500" MinHeight="100">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="5*"></DataGridTextColumn>
                    <DataGridTextColumn Header="ФИО" Width="20*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Пол" Width="10*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Дата рождения" Width="15*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Мобильный телефон" Width="20*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Email" Width="15*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Должность" Width="15*"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <DockPanel LastChildFill="True" HorizontalAlignment="Left">
                <Button Content="Добавить" DockPanel.Dock="Left" Width="100" Margin="10"></Button>
                <Button Content="Удалить" DockPanel.Dock="Right" Width="100" Margin="10"></Button>
                <Button Content="Изменить" Width="100" Margin="10"></Button>
            </DockPanel>
        </StackPanel>

        <DockPanel Grid.Row="1" LastChildFill="True">
            <Button x:Name="BackButton" Content="Назад" DockPanel.Dock="Left" Width="100" Click="BackButton_Click"></Button>
            <Label></Label>
        </DockPanel>
    </Grid>
</Window>
